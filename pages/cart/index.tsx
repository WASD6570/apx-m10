import { Layout } from "components/layout";
import { CartPage } from "components/cart-content";

const Cart = () => {
  return (
    <Layout>
      <CartPage />
    </Layout>
  );
};

export default Cart;
