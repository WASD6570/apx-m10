import { Layout } from "components/layout";
import { WaitingComponent } from "components/waiting-for-payment";
const WaitingForPaiment = () => {
  return (
    <Layout>
      <WaitingComponent />
    </Layout>
  );
};

export default WaitingForPaiment;
